# Dependencias Python necesarias para el proyecto rodi_ai
# Algunas de estas (geopandas/osmnx/fiona) requieren dependencias del sistema (GDAL/PROJ).
# Si la instalación con pip falla, instala primero los paquetes del sistema indicados

osmnx>=1.3.0
geopandas>=0.13.0
contextily>=1.2.0
matplotlib>=3.5
networkx>=2.8
pandas>=1.5
shapely>=2.0
simpy>=4.1.0
requests>=2.28
beautifulsoup4>=4.11.1
lxml>=4.9.2
fiona>=1.9.0
pyproj>=3.4
rtree>=0.9

# Opcionales útiles
jupyterlab
pytest

# Proxy server for CORS (used only if you run the local proxy)
Flask>=2.0
Flask-Cors>=3.0
requests>=2.28
